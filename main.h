#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <limits.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _strlen(char *s);
int _printf(const char *format, ...);
int __putchar(char c);

#endif
